# HP-Socket Demo Collection

This directory hosts standalone demonstrations that showcase how to build small utilities on top of the HP-Socket repository. Each subdirectory focuses on one scenario and contains its own build and usage instructions.

* `socks5/` – Minimal TCP Socks5 proxy with username/password authentication.
